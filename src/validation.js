export default (file) => file.endsWith('.json') || file.endsWith('.yaml');


//todo: validation file extension;
// validation file structure
//err: fileName has wrong extension; fileName has wrong structure